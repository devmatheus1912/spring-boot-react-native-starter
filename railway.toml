[build]
builder = "NIXPACKS"
buildCommand = "./gradlew clean build -x test"

[deploy]
startCommand = "java -jar build/libs/treinosdietas-0.0.1-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"

[variables]
NIXPACKS_JDK_VERSION = "17"